name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    # docs: https://github.com/telekom-mms/.github#release
    uses: telekom-mms/.github/.github/workflows/release.yml@0c3f6c78a37c4227f48ddf7fe9f8aca7798de14e # main
    secrets:
      GH_BRANCH_PROTECTION_APP_TOKEN: ${{ secrets.GH_BRANCH_PROTECTION_APP_TOKEN }}
